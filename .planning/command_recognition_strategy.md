# 命令识别策略评估

## 三种扩展方案对比

### 方案A：静态命令列表扩充
**原理**：在代码中硬编码更多命令

**优点**：
- ✅ 实现最简单
- ✅ 性能最好（O(1)查找）
- ✅ 无额外依赖
- ✅ 离线可用

**缺点**：
- ❌ 无法覆盖所有命令（Linux有上千个命令）
- ❌ 需要手动维护
- ❌ 无法识别用户自定义脚本

**适用场景**：
- MVP阶段
- 覆盖90%常用命令即可

---

### 方案B：AI辅助判断（疑似命令询问AI）
**原理**：当无法确定时，调用AI API判断是否为命令

**工作流程**：
```
用户复制文本 → 初步检查
  ├─ 明确是命令 → 直接解释
  ├─ 明确不是命令 → 忽略
  └─ 疑似命令 → 询问AI → 是 → 解释 / 否 → 忽略
```

**优点**：
- ✅ 能识别任何命令（包括自定义脚本）
- ✅ 识别准确率高
- ✅ 无需维护命令列表

**缺点**：
- ❌ 每次疑似命令都要调用AI（成本和延迟）
- ❌ 需要网络连接
- ❌ 可能频繁误判（如 `build.sh` 这种自定义脚本）

**成本估算**：
- 每次判断约100 tokens
- DeepSeek: $0.00014/次
- GPT-4: $0.003/次
- 假设每天触发10次疑似命令 → 月成本 $0.04 - $0.9

**适用场景**：
- 专业用户，频繁使用自定义命令
- 不在意AI调用成本

---

### 方案C：可配置命令列表
**原理**：在配置文件中添加用户自定义命令

**配置示例**：
```yaml
# config.yaml
capturer:
  custom_commands:
    # 用户自定义命令
    - mycommand
    - deploy
    - build
    - release
  
  # 是否启用宽松模式（疑似命令也触发）
  lenient_mode: false
```

**优点**：
- ✅ 灵活性高
- ✅ 零额外成本
- ✅ 用户可控
- ✅ 离线可用

**缺点**：
- ⚠️ 需要用户手动配置
- ⚠️ 首次使用体验不佳（需要逐个添加）

**适用场景**：
- 与方案A结合使用
- 作为补充方案

---

## 🎯 推荐策略：混合方案

### 核心思路
**三层识别机制**：

```
┌─────────────────────────────────────────┐
│  用户复制文本                            │
└─────────────┬───────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│  第一层：静态命令列表（80+ 常用命令）    │ ← 覆盖90%场景
│  - 快速、离线、免费                      │
└─────────────┬───────────────────────────┘
              │ 未命中
              ▼
┌─────────────────────────────────────────┐
│  第二层：用户自定义列表（config.yaml）   │ ← 个性化
│  - 项目特定命令、别名                    │
└─────────────┬───────────────────────────┘
              │ 未命中
              ▼
┌─────────────────────────────────────────┐
│  第三层：智能启发式 + 可选AI判断         │
│  - 检查特征（路径、扩展名、参数）         │
│  - 可选：询问AI（用户配置是否启用）       │
└─────────────────────────────────────────┘
```

---

## 📋 实施方案

### 阶段一：扩充静态列表（立即实施）

**目标**：从80个增加到150+个常用命令

**新增类别**：
```python
# 1. 编辑器
'vim', 'nvim', 'nano', 'emacs', 'code', 'subl', 'gedit',

# 2. 版本控制
'svn', 'hg', 'fossil', 'bzr',

# 3. 构建工具
'mvn', 'gradle', 'ant', 'sbt', 'bazel', 'ninja',

# 4. 数据库
'mysql', 'psql', 'sqlite3', 'mongo', 'redis-cli',

# 5. 监控工具
'watch', 'strace', 'lsof', 'netcat', 'tcpdump', 'wireshark',

# 6. 容器/虚拟化
'vagrant', 'virtualbox', 'lxc', 'qemu',

# 7. 包管理（语言特定）
'gem', 'bundler', 'composer', 'nuget', 'mix', 'pub',

# 8. 系统工具
'killall', 'pkill', 'crontab', 'at', 'batch', 'nice', 'renice',
'nohup', 'screen', 'tmux', 'bg', 'fg', 'jobs',

# 9. 磁盘工具
'fdisk', 'parted', 'mkfs', 'mount', 'umount', 'fsck',

# 10. 用户管理
'useradd', 'userdel', 'usermod', 'groupadd', 'passwd', 'su',
```

**评估**：
- 工作量：⭐ 低（5分钟）
- 覆盖率：从90% → 95%
- 优先级：🔴 高

---

### 阶段二：实现可配置列表（推荐下一步）

**配置文件设计**：
```yaml
# config.yaml
capturer:
  # 用户自定义命令（会被添加到识别列表）
  custom_commands:
    - mycommand
    - deploy
    - build
  
  # 用户自定义别名映射（可选）
  command_aliases:
    dc: docker-compose
    k: kubectl
    g: git
  
  # 识别模式
  recognition_mode: strict  # strict | lenient | ai_assisted
  
  # AI辅助判断（仅在 ai_assisted 模式下生效）
  ai_fallback:
    enabled: false
    max_calls_per_hour: 10  # 防止成本失控
```

**实现代码**：
```python
# src/capturer/clipboard.py

def is_command(self, content: Optional[str]) -> bool:
    # ... 现有逻辑 ...
    
    # 检查用户自定义命令
    custom_commands = self._config.get('capturer', {}).get('custom_commands', [])
    if first_word in custom_commands:
        return True
    
    # 检查别名
    aliases = self._config.get('capturer', {}).get('command_aliases', {})
    if first_word in aliases:
        return True
    
    # 根据模式决定是否使用AI判断
    mode = self._config.get('capturer', {}).get('recognition_mode', 'strict')
    
    if mode == 'lenient':
        # 宽松模式：任何看起来像命令的都算
        return self._is_possible_command(content)
    elif mode == 'ai_assisted':
        # AI辅助模式
        return self._ask_ai_if_command(content)
    
    return False
```

**评估**：
- 工作量：⭐⭐ 中（1小时）
- 用户体验：⭐⭐⭐⭐
- 优先级：🟡 中

---

### 阶段三：AI辅助判断（可选，谨慎使用）

**实现方式**：

```python
def _ask_ai_if_command(self, content: str) -> bool:
    """使用AI判断是否为命令（需要用户显式启用）"""
    
    # 检查是否启用
    if not self._config.get('capturer', {}).get('ai_fallback', {}).get('enabled', False):
        return False
    
    # 频率限制（防止成本失控）
    if self._ai_call_count_this_hour >= self._max_ai_calls:
        logging.warning("AI判断调用次数已达上限，跳过")
        return False
    
    # 缓存检查（避免重复询问相同内容）
    cache_key = hashlib.md5(content.encode()).hexdigest()
    if cache_key in self._ai_judgment_cache:
        return self._ai_judgment_cache[cache_key]
    
    # 调用AI（使用极简Prompt减少成本）
    prompt = f"Is this a shell command? Answer only 'yes' or 'no': {content}"
    response = self._ai_client.complete(prompt, max_tokens=5)
    
    is_command = response.strip().lower() == 'yes'
    
    # 缓存结果
    self._ai_judgment_cache[cache_key] = is_command
    self._ai_call_count_this_hour += 1
    
    return is_command
```

**成本控制**：
- 每小时最多10次调用
- 使用最便宜的模型（DeepSeek）
- 缓存结果（避免重复询问）
- 极简Prompt（5 tokens响应）

**评估**：
- 成本：月均 < $0.05
- 识别准确率：接近100%
- 优先级：🟢 低（非必需）

---

## 🎨 用户体验优化

### 疑似命令提示

当遇到疑似命令但无法确定时，可以提示用户：

```
[DEBUG] 疑似命令: 'myapp --config prod'
        第一个词 'myapp' 不在已知命令列表中。

💡 提示：如果这是一个命令，您可以：
   1. 添加到配置文件：capturer.custom_commands
   2. 启用AI辅助判断（需要配置 API）
   3. 按任意键忽略
```

### 学习模式（未来功能）

记录用户确认过的命令，自动添加到自定义列表：

```python
def on_user_confirmed(command: str):
    """用户确认这是一个命令后，自动学习"""
    first_word = command.split()[0]
    
    # 添加到用户配置
    config = load_config()
    if first_word not in config['capturer']['custom_commands']:
        config['capturer']['custom_commands'].append(first_word)
        save_config(config)
        
        print(f"✓ 已将 '{first_word}' 添加到自定义命令列表")
```

---

## 📊 方案对比总结

| 方案 | 覆盖率 | 成本 | 性能 | 实现难度 | 推荐度 |
|:----:|:------:|:----:|:----:|:--------:|:------:|
| 扩充静态列表 | 95% | 免费 | ⭐⭐⭐⭐⭐ | ⭐ 低 | ⭐⭐⭐⭐⭐ |
| 可配置列表 | 98% | 免费 | ⭐⭐⭐⭐⭐ | ⭐⭐ 中 | ⭐⭐⭐⭐ |
| AI辅助判断 | 99.9% | $0.05/月 | ⭐⭐⭐ | ⭐⭐⭐ 高 | ⭐⭐ |
| **混合方案** | **99%** | **免费** | **⭐⭐⭐⭐** | **⭐⭐** | **⭐⭐⭐⭐⭐** |

---

## 🎯 最终推荐

### 立即实施
1. **扩充静态命令列表** - 从80个增加到150+个
2. **实现可配置列表** - 支持 `custom_commands` 配置项

### 未来考虑
3. **AI辅助判断** - 作为可选功能，默认禁用
4. **学习模式** - 自动记录用户确认的命令

### 配置示例（推荐）

```yaml
# config.yaml
capturer:
  mode: keyboard  # keyboard | clipboard
  hotkey: ctrl+shift+E
  
  # 用户自定义命令
  custom_commands:
    - deploy
    - build
    - mycommand
  
  # 识别模式
  recognition:
    mode: strict  # strict | lenient | ai_assisted
    
    # AI辅助（可选，默认禁用）
    ai_fallback:
      enabled: false
      provider: deepseek  # 使用最便宜的模型
      max_calls_per_hour: 10
```

---

## 💡 额外建议

### 1. 添加"未知命令"反馈机制

当用户多次尝试某个未识别的命令时：

```
⚠️ 检测到您多次尝试 'myapp'，但它不在已知命令列表中。

是否将 'myapp' 添加到自定义命令列表？ [Y/n]
```

### 2. 命令使用统计

记录哪些命令最常被解释，用于优化静态列表：

```python
# ~/.cli-explainer/stats.json
{
  "commands": {
    "docker": 127,
    "git": 98,
    "myapp": 45  # ← 如果频繁使用自定义命令，提示添加到列表
  }
}
```

### 3. 社区共享命令库

允许用户下载社区维护的命令列表：

```bash
# 下载 k8s 相关命令列表
cli-explainer install-commands kubernetes

# 下载 AWS CLI 相关命令
cli-explainer install-commands aws
```

---

## 总结

**最佳实践**：
1. **现在**：扩充静态列表到150+个（覆盖95%）
2. **下一步**：实现可配置自定义列表（覆盖98%）
3. **未来**：考虑AI辅助作为可选高级功能（覆盖99.9%）

**关键原则**：
- 🎯 性能优先（静态列表最快）
- 💰 成本可控（AI调用需严格限制）
- 🔧 灵活配置（用户可根据需求选择模式）
- 📚 持续学习（记录用户习惯，优化列表）
