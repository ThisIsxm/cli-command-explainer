# 快速开始指南

## 1. 配置 API 密钥

1. 编辑 `.env`:
```bash
API_KEY=sk-your-actual-key-here
```

2. 编辑 `config/config.yaml`:
```yaml
ai:
  api_base: https://openrouter.ai/api/v1
  model: openai/gpt-oss-120b
```

## 2. 测试 AI 链路

运行测试脚本确保一切正常:

```bash
# Windows
cli-venv\Scripts\python test_ai_chain.py

# Linux/Mac
source cli-venv/bin/activate
python test_ai_chain.py
```

如果看到 "✅ AI 链路测试成功！"，说明配置正确。

## 3. 运行程序

### 模式 1: 单命令模式（快速测试）

```bash
python -m src.main "ls -la"
python -m src.main "rm -rf /tmp/test"
python -m src.main "git push --force"
```

###  模式 2: 交互模式（推荐学习使用）

```bash
python -m src.main --interactive
```

然后输入命令:
```
$ ls -la
$ npm install lodash
$ sudo apt update
$ quit  # 退出
```

### 模式 3: 剪贴板监听模式（推荐日常使用）

```bash
python -m src.main --clipboard
```

使用方法:
1. 程序启动后在后台运行
2. 复制任何命令到剪贴板（Ctrl+C）
3. 按 `Ctrl+Shift+E` 触发解释
4. 终端会显示详细解释

## 4. 示例输出

```
命令解释
═══════════════════════════════════════════════════

█ 命令 █
ls -la

概要:
  列出当前目录下的所有文件和文件夹（包括隐藏文件）并显示详细信息

详细说明:
  ls 命令用于列出目录内容。-l 选项表示使用长格式显示详细信息（权限、所有者、大小等），
  -a 选项表示显示所有文件（包括以.开头的隐藏文件）

用途:
  查看目录内容和文件详细信息

风险评估
┌─────────────────────────────────────┐
│ 🟢 低风险                            │
│ 评分: 5.0/100                       │
│                                     │
│ 建议: 该命令安全，可以放心执行       │
└─────────────────────────────────────┘
```

## 5. 常见问题

### AI 服务不可用

如果看到 "AI 服务不可用" 提示:

1. **检查 LiteLLM 是否安装**:
```bash
pip install litellm
```

2. **检查 API Key 是否设置**:
```bash
# Windows
echo %DEEPSEEK_API_KEY%

# Linux/Mac
echo $DEEPSEEK_API_KEY
```

3. **检查网络连接**

### 测试失败

查看详细日志:
```bash
python -m src.main "ls" --verbose
```

## 6. 高级配置

编辑 `config/config.yaml`:

```yaml
ai:
  provider: deepseek        # openai, deepseek, openrouter, ollama
  model: deepseek-chat      # 模型名称
  api_base: https://api.deepseek.com  # API 端点
  timeout: 30               # 请求超时（秒）
  max_retries: 3            # 最大重试次数

hotkey:
  trigger: ctrl+shift+e     # 触发快捷键

display:
  language: zh              # zh 或 en
  show_details: true        # 显示详细信息
  show_emoji: true          # 显示 emoji
  risk_color: true          # 风险颜色编码
```

## 7. 开发模式

运行测试:
```bash
pytest tests/ -v
```

查看代码审查报告:
```bash
cat CODE_REVIEW.md
```

查看进度报告:
```bash
cat PROGRESS.md
```
