# 贡献指南

感谢你考虑为 CLI Command Explainer 做出贡献！

## 🚀 快速开始

### 1. Fork 并克隆仓库

```bash
git clone https://github.com/ThisIsxm/cli-command-explainer.git
cd cli-command-explainer
```

### 2. 创建开发环境

```bash
python -m venv cli-venv
source cli-venv/bin/activate  # Linux/Mac
# cli-venv\Scripts\activate   # Windows

pip install -r requirements.txt
pip install -e ".[dev]"  # 安装开发依赖
```

### 3. 创建分支

```bash
git checkout -b feature/your-feature-name
```

## 📝 提交规范

### Commit Message 格式

```
<type>(<scope>): <subject>

<body>

<footer>
```

#### Type 类型
- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式（不影响功能）
- `refactor`: 重构
- `test`: 测试相关
- `chore`: 构建/工具配置

#### 示例
```
feat(explainer): 添加本地命令数据库支持

新增离线模式，当AI服务不可用时使用本地数据库

Closes #123
```

## 🧪 测试

### 运行测试

```bash
# 运行所有测试
pytest tests/ -v

# 运行特定测试
pytest tests/test_parser.py -v

# 查看覆盖率
pytest tests/ --cov=src --cov-report=html
```

### 添加测试

请为新功能添加测试用例：

```python
# tests/test_your_feature.py
def test_new_feature():
    # 测试代码
    assert result == expected
```

## 🎨 代码规范

### 格式化

使用 Black 格式化代码：

```bash
black src/ tests/
```

### Linting

```bash
flake8 src/ tests/
```

### 类型检查（可选）

```bash
mypy src/
```

## 📋 Pull Request 流程

1. **确保测试通过**
   ```bash
   pytest tests/ -v
   ```

2. **更新文档**
   - 如果添加新功能，更新 README.md
   - 在 CHANGELOG.md 的 `[Unreleased]` 部分记录变更

3. **提交 PR**
   - 标题清晰描述变更
   - 描述中说明：
     - 改动内容
     - 解决的问题
     - 相关 Issue 编号

4. **代码审查**
   - 等待维护者审查
   - 根据反馈进行修改

## 🐛 报告 Bug

在 [GitHub Issues](https://github.com/ThisIsxm/cli-command-explainer/issues) 提交 Bug 时请包含：

- **环境信息**：操作系统、Python 版本
- **复现步骤**：详细的步骤
- **预期行为**：应该发生什么
- **实际行为**：实际发生了什么
- **日志信息**：相关的错误日志

### Bug 报告模板

```markdown
**环境**
- OS: Windows 11
- Python: 3.11.0
- 版本: 1.0.0

**复现步骤**
1. 运行 `python -m src.main "some command"`
2. 按下 Ctrl+C
3. 观察到...

**预期行为**
应该...

**实际行为**
却...

**日志**
```
...
```
```

## 💡 功能建议

在 [GitHub Discussions](https://github.com/ThisIsxm/cli-command-explainer/discussions) 讨论新功能想法。

请包含：
- 用例描述
- 为什么需要这个功能
- 可能的实现方式

## 📂 项目结构

```
cli-command-explainer/
├── src/
│   ├── capturer/       # 命令捕获模块
│   │   ├── base.py         # 基类定义
│   │   ├── clipboard.py    # 剪贴板捕获
│   │   └── hotkey.py       # 热键管理
│   ├── parser/         # 命令解析模块
│   ├── risk/           # 风险评估模块
│   ├── explainer/      # AI 解释引擎
│   ├── ui/             # UI 展示模块
│   ├── config.py       # 配置管理
│   └── main.py         # 程序入口
├── tests/              # 测试目录
├── .planning/          # 开发文档
└── config/             # 配置文件
```

## 🎯 开发优先级

当前重点功能（按优先级）：

1. **高优先级**
   - 扩展命令识别库
   - 用户自定义命令
   - 执行确认交互

2. **中优先级**
   - GUI 弹窗模式
   - 命令历史记录
   - 性能优化

3. **低优先级**
   - 多语言支持（日语、韩语等）
   - 主题定制
   - 插件系统

## 🤝 行为准则

- 尊重所有贡献者
- 接受建设性批评
- 专注于对项目最有利的事情

## 📞 联系方式

- 技术问题：GitHub Issues
- 功能讨论：GitHub Discussions
- 其他事宜：thisis_xm@foxmail.com

---

**再次感谢你的贡献！** 🎉
